version: 2

models:
  - name: int_local_bike__products
    description: '{{ doc("int_local_bike__products") }}'
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
          - unique
          - not_null